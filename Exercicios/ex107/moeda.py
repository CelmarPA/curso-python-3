def metade(val):
    valor = val / 2
    return valor


def dobro(val):
    valor = val * 2
    return valor


def aumentar(val, por):
    valor = (val + (val * por / 100))
    return valor


def diminuir(val, por):
    valor = (val - (val * por / 100))
    return valor
